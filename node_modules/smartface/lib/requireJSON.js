const fs = require('fs');
const path = require('path');
const jsonlint = require("jsonlint");

module.exports = function(dirname, filepath){
	const fileResolvedPath = (!filepath) ? dirname : path.normalize(path.join(dirname, filepath));
	return jsonlint.parse(fs.readFileSync(fileResolvedPath, 'utf8'));
};
